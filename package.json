{
  "name": "elite-lifestyle-london",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "@astrojs/preact": "^1.0.2",
    "@astrojs/sitemap": "^1.0.0",
    "astro": "^2.0.0",
    "preact": "^10.26.8"
  },
  "devDependencies": {
    "@netlify/netlify-marketing-tokens": "^1.1.0"
  }
}
